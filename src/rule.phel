(ns tic-tac-toe\rule)

(defn create-matrix []
  [[0 0 0]
   [0 0 0]
   [0 0 0]])

(def- value-map {0 " "
                 1 "○"
                 2 "×"})

(defn matrix-cell-char [matrix {:x x :y y}]
  (let [value (get-in matrix [x y])]
    (or (value-map value) "_")))
